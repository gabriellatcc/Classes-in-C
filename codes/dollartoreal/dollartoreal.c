#include <stdio.h>
int main()
{
    float d,p,r;
    printf("Enter the current dollar price: ");
    scanf("%f",&d);
    printf("Enter the product price in dollars: ");
    scanf("%f",&p);
    printf("Converted value in reais: %.2f",r);

      return(0);
}
