#include <stdio.h>
    #define MAX 100
    int display()
    {
    return (MAX);
    }
    int main()
    {
        int x=1,const;
            printf("X value= %d\n",x);
            const=exibir();
            printf("X value Max= %d",const);
    return(0);
    }
