#include <stdio.h>
int main ()
{
    int v, t, d;
        printf("Enter the velocity value: ");
        scanf("%d",&v);
        printf("Enter the time value: ");
        scanf("%d",&t);
        d=v*t;
        printf("Distance: %d\n", d);
return 0;
}
